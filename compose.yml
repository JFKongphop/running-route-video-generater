version: '3.8'

services:
  runarium:
    build: .
    ports:
      - "3000:3000"
    environment:
      - RUST_LOG=info
    # Optional: limit resources
    deploy:
      resources:
        limits:
          memory: 2G
        reservations:
          memory: 512M
    restart: unless-stopped
